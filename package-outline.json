{
  "name": "local-ai-assistant",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first desktop AI assistant for Windows and macOS with permissions, voice, memory, email and WhatsApp connector boundaries.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "notes": [
    "This is an outline file, not the final package.json.",
    "Use CLAUDE_BUILD_PROMPT.md to generate the implementation."
  ]
}

